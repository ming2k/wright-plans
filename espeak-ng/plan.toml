name = "espeak-ng"
version = "1.52.0"
release = 1
description = "Open source speech synthesizer that supports more than 100 languages"
license = "GPL-3.0-or-later"
arch = "x86_64"
url = "https://github.com/espeak-ng/espeak-ng"


build_deps = ['ninja', 'cmake']

[[sources]]
type = "http"
sha256 = "SKIP"
url = "https://github.com/espeak-ng/espeak-ng/archive/refs/tags/${VERSION}.tar.gz"
extract_to = "source"

[lifecycle.configure]
script = '''
cd source/espeak-ng-${VERSION}
cmake -B build -G Ninja \
  -DCMAKE_BUILD_TYPE=Release \
  -DCMAKE_INSTALL_PREFIX=/usr \
  -DBUILD_SHARED_LIBS=ON \
  -DUSE_MBROLA=OFF \
  -DUSE_LIBSONIC=OFF \
  -DUSE_LIBPCAUDIO=OFF \
  -DUSE_ASYNC=OFF'''

[lifecycle.compile]
script = '''
cd source/espeak-ng-${VERSION}
ninja -C build -j$(nproc)'''

[lifecycle.staging]
script = '''
cd source/espeak-ng-${VERSION}
DESTDIR=${STAGING_DIR} ninja -C build install'''

