name = "spirv-llvm-translator"
version = "22.1.2"
release = 1
description = "Bi-directional translation between SPIR-V and LLVM IR"
license = "UoI-NCSA"
arch = "x86_64"
url = "https://github.com/KhronosGroup/SPIRV-LLVM-Translator"

# v22.1.3 adds SPV_INTEL_predicated_io support which requires spirv-headers
# newer than Vulkan SDK 1.4.350.1. v22.1.2 is the latest version compatible
# with SDK 1.4.350.1's spirv-headers snapshot.
link_deps = ['glibc', 'gcc:gcc-libs', 'gcc:libstdc++', 'llvm:llvm-libs']
build_deps = ['cmake']

[[sources]]
type = "http"
sha256 = "b37196b1a1a60282a24cf937ab7d6807d7d54dc718f2a37a78e211be26df57ac"
url = "https://github.com/KhronosGroup/SPIRV-LLVM-Translator/archive/refs/tags/v${VERSION}.tar.gz"
extract_to = "source"

[options]
ccache = true

[pipeline.configure]
script = '''
cd source/SPIRV-LLVM-Translator-${VERSION}
cmake -B build -DCMAKE_INSTALL_PREFIX=/usr -DCMAKE_BUILD_TYPE=Release -DCMAKE_INSTALL_LIBDIR=lib -DBUILD_SHARED_LIBS=ON -DLLVM_EXTERNAL_SPIRV_HEADERS_SOURCE_DIR=/usr -DLLVM_SPIRV_INCLUDE_TESTS=OFF'''

[pipeline.compile]
script = '''
cd source/SPIRV-LLVM-Translator-${VERSION}
cmake --build build -j$(nproc)'''

[pipeline.staging]
script = '''
cd source/SPIRV-LLVM-Translator-${VERSION}
DESTDIR=${STAGING_DIR} cmake --install build'''

[[output]]
runtime_deps = ['glibc', 'gcc:gcc-libs', 'gcc:libstdc++', 'llvm:llvm-libs']
