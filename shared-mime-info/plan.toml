name = "shared-mime-info"
version = "2.4"
release = 1
description = "Shared MIME info database"
license = "GPL-2.0-or-later"
arch = "x86_64"
url = "https://gitlab.freedesktop.org/xdg/shared-mime-info"

[[sources]]
uri = "https://gitlab.freedesktop.org/xdg/shared-mime-info/-/archive/${PART_VERSION}/shared-mime-info-${PART_VERSION}.tar.gz"
sha256 = "SKIP"

[lifecycle.configure]
script = '''
meson setup ${BUILD_DIR}/build ${BUILD_DIR} \
    --prefix=/usr          \
    --buildtype=release    \
    -D update-mimedb=false

'''

[lifecycle.compile]
script = "ninja -C ${BUILD_DIR}/build -j$(nproc)"

[lifecycle.staging]
script = "DESTDIR=${PART_DIR} ninja -C ${BUILD_DIR}/build install"

[hooks]
post_install = "update-mime-database /usr/share/mime 2>/dev/null || true"
post_upgrade = "update-mime-database /usr/share/mime 2>/dev/null || true"

[lifecycle.fabricate]
