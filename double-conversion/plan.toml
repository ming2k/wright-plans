name = "double-conversion"
version = "3.3.0"
release = 1
description = "Binary-decimal and decimal-binary conversion routines for IEEE doubles"
license = "BSD-3-Clause"
arch = "x86_64"
url = "https://github.com/google/double-conversion"

[dependencies]
build = ["cmake", "gcc", "ninja"]

[[sources]]
uri = "https://github.com/google/double-conversion/archive/refs/tags/v${PART_VERSION}.tar.gz"
sha256 = "SKIP"

[lifecycle.configure]
script = '''
cd ${BUILD_DIR}
cmake -B build -G Ninja -DCMAKE_INSTALL_PREFIX=/usr -DCMAKE_BUILD_TYPE=Release -DCMAKE_POLICY_VERSION_MINIMUM=3.5 -DBUILD_SHARED_LIBS=ON -DBUILD_TESTING=OFF

'''

[lifecycle.compile]
script = '''
cd ${BUILD_DIR}
ninja -C build

'''

[lifecycle.staging]
script = '''
cd ${BUILD_DIR}
DESTDIR=${PART_DIR} ninja -C build install

'''

[lifecycle.fabricate]
