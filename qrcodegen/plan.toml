name = "qrcodegen"
version = "1.8.0"
release = 1
description = "QR Code generator library for C and C++"
license = "MIT"
arch = "x86_64"
url = "https://www.nayuki.io/page/qr-code-generator-library"


[[sources]]
type = "http"
sha256 = "SKIP"
url = "https://github.com/nayuki/QR-Code-generator/archive/refs/tags/v${VERSION}.tar.gz"
extract_to = "source"

[pipeline.compile]
script = '''
cd source/c
gcc -c -O2 -fPIC -o qrcodegen.o qrcodegen.c
gcc -shared -o libqrcodegen.so qrcodegen.o

cd source/cpp
g++ -c -O2 -fPIC -std=c++17 -o qrcodegen.o qrcodegen.cpp
g++ -shared -o libqrcodegencpp.so qrcodegen.o'''

[pipeline.staging]
script = '''
cd source/qrcodegen-${VERSION}
install -Dm755 ${WORKDIR}/c/libqrcodegen.so ${STAGING_DIR}/usr/lib/libqrcodegen.so
install -Dm644 ${WORKDIR}/c/qrcodegen.h ${STAGING_DIR}/usr/include/qrcodegen.h

install -Dm755 ${WORKDIR}/cpp/libqrcodegencpp.so ${STAGING_DIR}/usr/lib/libqrcodegencpp.so
install -Dm644 ${WORKDIR}/cpp/qrcodegen.hpp ${STAGING_DIR}/usr/include/qrcodegen.hpp'''

