[plan]
name = "e2fsprogs"
version = "1.47.3"
release = 3
description = "Ext2/3/4 filesystem utilities"
license = "GPL-2.0-only"
arch = "x86_64"
url = "https://e2fsprogs.sourceforge.net"

[dependencies]
build = ["util-linux"]

[[sources]]
uri = "https://kernel.org/pub/linux/kernel/people/tytso/e2fsprogs/v${PART_VERSION}/e2fsprogs-${PART_VERSION}.tar.xz"
sha256 = "SKIP"

[lifecycle.configure]
script = '''
cd ${BUILD_DIR}
mkdir -p build && cd build
../configure --prefix=/usr          \
--with-root-prefix=''   \
--libdir=/usr/lib       \
--libexecdir=/usr/lib   \
--sbindir=/usr/bin      \
--enable-elf-shlibs     \
--disable-libblkid      \
--disable-libuuid       \
--disable-uuidd         \
--disable-fsck
'''

[lifecycle.compile]
script = '''
cd ${BUILD_DIR}/build
make -j$(nproc)
'''

[lifecycle.staging]
script = '''
cd ${BUILD_DIR}/build
DESTDIR=${PART_DIR} make install
DESTDIR=${PART_DIR} make install-libs

# Remove static libraries
rm -f ${PART_DIR}/usr/lib/lib{com_err,e2p,ext2fs,ss}.a
'''
