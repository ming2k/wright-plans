[plan]
name = "libfuse3"
version = "3.18.1"
release = 6
description = "Filesystem in Userspace"
license = "GPL-2.0-only AND LGPL-2.1-only"
arch = "x86_64"
url = "https://github.com/libfuse/libfuse"

[[sources]]
uri = "https://github.com/libfuse/libfuse/releases/download/fuse-${PART_VERSION}/fuse-${PART_VERSION}.tar.gz"
sha256 = "SKIP"

[lifecycle.configure]
script = '''
meson setup ${BUILD_DIR}/build ${BUILD_DIR} \
  --prefix=/usr          \
  --sbindir=/usr/bin     \
  --buildtype=release    \
  -D tests=false         \
  -D examples=false      \
  -D useroot=false
'''

[lifecycle.compile]
script = "ninja -C ${BUILD_DIR}/build -j$(nproc)"

[lifecycle.staging]
backup = ["/etc/fuse.conf"]
script = '''
DESTDIR=${PART_DIR} ninja -C ${BUILD_DIR}/build install

# Remove legacy init scripts and static device nodes (handled by udev)
rm -rf ${PART_DIR}/etc/init.d ${PART_DIR}/dev 2>/dev/null || true

# Allow non-root users to mount FUSE filesystems via fusermount3
chmod u+s ${PART_DIR}/usr/bin/fusermount3
'''
