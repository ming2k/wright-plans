[plan]
name = "ffmpeg"
version = "8.0.1"
release = 4
description = "Complete, cross-platform solution to record, convert and stream audio and video"
license = "GPL-2.0-or-later"
arch = "x86_64"
url = "https://ffmpeg.org"

[dependencies]
build = ["gcc", "make", "nasm", "pkgconf", "sdl2"]
link = [
    "alsa-lib",
    "aom",
    "dav1d",
    "freetype",
    "gnutls",
    "libass",
    "libdrm",
    "libglvnd",
    "libvorbis",
    "libvpx",
    "libwebp",
    "libxml2",
    "mesa",
    "opus",
    "pipewire",
    "sdl2",
    "vulkan-loader",
    "x264",
    "x265",
    "zlib",
    "bzip2",
    "lz4",
    "zstd",
]

[[sources]]
uri = "https://ffmpeg.org/releases/ffmpeg-${PART_VERSION}.tar.xz"
sha256 = "SKIP"

[lifecycle.configure]
script = '''
cd ${BUILD_DIR}

pkg-config --exists sdl2 || {
  echo "ffplay requested but SDL2 pkg-config metadata is missing"
  exit 1
}

command -v sdl2-config >/dev/null 2>&1 || {
  echo "ffplay requested but sdl2-config is missing from the build environment"
  exit 1
}

./configure \
  --prefix=/usr \
  --disable-debug \
  --disable-static \
  --disable-stripping \
  --enable-shared \
  --enable-gpl \
  --enable-version3 \
  --enable-gnutls \
  --enable-libass \
  --enable-libaom \
  --enable-libdav1d \
  --enable-libfreetype \
  --enable-libvorbis \
  --enable-libvpx \
  --enable-libwebp \
  --enable-libx264 \
  --enable-libx265 \
  --enable-libxml2 \
  --enable-libopus \
  --enable-sdl2 \
  --enable-ffplay \
  --enable-vulkan \
  --enable-libdrm \
  --enable-lto \
  --enable-pic
'''

[lifecycle.compile]
script = '''
cd ${BUILD_DIR}
make -j$(nproc)
'''

[lifecycle.staging]
script = '''
cd ${BUILD_DIR}
make DESTDIR=${PART_DIR} install

test -x ${PART_DIR}/usr/bin/ffplay
'''
