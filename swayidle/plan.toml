name = "swayidle"
version = "1.9.0"
release = 1
description = "Idle management daemon for Wayland"
license = "MIT"
arch = "x86_64"
url = "https://github.com/swaywm/swayidle"

[dependencies]
runtime = ["systemd"]
build = ["meson", "ninja"]
link = ["wayland", "wayland-protocols"]

[[sources]]
uri = "https://github.com/swaywm/swayidle/releases/download/v${PART_VERSION}/swayidle-${PART_VERSION}.tar.gz"
sha256 = "6c1b769038b60250c88e47380cbb021cfa57a65f872bf4d6c340b5e3057096ac"

[lifecycle.configure]
script = '''
cd ${BUILD_DIR}
meson setup build --prefix=/usr --buildtype=release -Dlogind=enabled -Dlogind-provider=systemd -Dman-pages=disabled

'''

[lifecycle.compile]
script = '''
cd ${BUILD_DIR}
ninja -C build

'''

[lifecycle.staging]
script = '''
cd ${BUILD_DIR}
DESTDIR=${PART_DIR} ninja -C build install

'''
