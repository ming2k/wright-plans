name = "ostree"
version = "2026.3"
release = 1
description = "Operating system and container binary deployment and upgrades"
license = "LGPL-2.0-or-later"
arch = "x86_64"
url = "https://ostreedev.github.io/ostree/"

link_deps = ['glib', 'libarchive', 'curl', 'gpgme']

[[sources]]
type = "http"
sha256 = "e560e47631d1f703e9ed3425e8909ccd87fa2992422c07348ca88ec98943c8fb"
url = "https://github.com/ostreedev/ostree/releases/download/v${VERSION}/libostree-${VERSION}.tar.xz"
extract_to = "source"

[pipeline.configure]
script = '''
cd source/libostree-${VERSION}
./configure --prefix=/usr       \
    --disable-static            \
    --with-curl                 \
    --without-soup              \
    --without-soup3             \
    --disable-man               \
    --disable-gtk-doc           \
    --with-gpgme                \
    --sysconfdir=/etc            \
    --disable-otmpfile          \
    --with-builtin-grub2-mkconfig'''

[pipeline.compile]
script = '''
cd source/libostree-${VERSION}
make -j$(nproc)'''

[pipeline.staging]
script = '''
cd source/libostree-${VERSION}
DESTDIR=${STAGING_DIR} make install'''

[[output]]
runtime_deps = ['systemd', 'glib', 'libarchive', 'curl', 'gpgme']
