name = "nano"
version = "8.6"
release = 1
description = "A small, simple text editor aiming to replace Pico"
license = "GPL-3.0-or-later"
arch = "x86_64"
url = "https://www.nano-editor.org/"

[dependencies]
build = ["gcc", "make"]
link = ["ncurses"]

[[sources]]
sha256 = "SKIP"
url = "https://www.nano-editor.org/dist/v8/nano-${VERSION}.tar.xz"
extract_to = "source"

[lifecycle.configure]
script = '''
cd source
./configure --prefix=/usr \
--sysconfdir=/etc \
--enable-utf8 \
--docdir=/usr/share/doc/nano-${VERSION}'''

[lifecycle.compile]
script = '''
cd source
make -j$(nproc)'''

[lifecycle.staging]
script = '''
cd source
make DESTDIR=${PART_DIR} install
install -v -m644 doc/{nano.html,sample.nanorc} ${PART_DIR}/usr/share/doc/nano-${VERSION}'''
