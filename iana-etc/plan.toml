name = "iana-etc"
version = "20250807"
release = 1
description = "Data for network services and protocols (/etc/services and /etc/protocols)"
license = "MIT"
arch = "x86_64"
url = "https://www.iana.org/protocols"

[[sources]]
uri = "https://github.com/Mic92/iana-etc/releases/download/${PART_VERSION}/iana-etc-${PART_VERSION}.tar.gz"
sha256 = "SKIP"

[lifecycle.staging]
script = '''
cd ${BUILD_DIR}
install -Dm644 services  ${PART_DIR}/etc/services
install -Dm644 protocols ${PART_DIR}/etc/protocols

'''

[output]
backup = ["/etc/services", "/etc/protocols"]
