[plan]
name = "wireless-regdb"
version = "2024.10.07"
release = 1
description = "Wireless regulatory database for cfg80211"
license = "ISC"
arch = "any"
url = "https://wireless.wiki.kernel.org/en/developers/regulatory/wireless-regdb"

[[sources]]
uri = "https://www.kernel.org/pub/software/network/wireless-regdb/wireless-regdb-${PART_VERSION}.tar.xz"
sha256 = "SKIP"

[lifecycle.staging]
script = '''
cd ${BUILD_DIR}
install -Dm644 regulatory.db ${PART_DIR}/usr/lib/firmware/regulatory.db
install -Dm644 regulatory.db.p7s ${PART_DIR}/usr/lib/firmware/regulatory.db.p7s
'''
