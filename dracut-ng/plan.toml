name = "dracut-ng"
version = "112"
release = 1
description = "Event-driven initramfs infrastructure with UKI support"
license = "GPL-2.0-or-later"
arch = "x86_64"
url = "https://github.com/dracut-ng/dracut-ng"


[[sources]]
type = "http"
sha256 = "839d21eb4bd27bb0ee40d74c9da46df088c2e1425fcd8c3ea62b7e3a3dd28a91"
url = "https://github.com/dracut-ng/dracut-ng/archive/${VERSION}/dracut-ng-${VERSION}.tar.gz"
extract_to = "source"

[pipeline.configure]
script = '''
cd source/dracut-ng-${VERSION}
./configure \
  --prefix=/usr \
  --libdir=/usr/lib \
  --sysconfdir=/etc \
  --sbindir=/usr/bin \
  --systemdsystemunitdir=/usr/lib/systemd/system \
  --disable-documentation'''

[pipeline.compile]
script = '''
cd source/dracut-ng-${VERSION}
make -j$(nproc)'''

[pipeline.staging]
script = '''
cd source/dracut-ng-${VERSION}
make DESTDIR=${STAGING_DIR} install'''

[[output]]
runtime_deps = ['bash', 'coreutils', 'cpio', 'kmod', 'systemd', 'util-linux']
